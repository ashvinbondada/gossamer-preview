.vscode/**
.vscode-test/**
.claude/**
.playwright-mcp/**
harness-*.png
.env
posthog-audit-report.md
src/**
docs/**
.gitignore
tsconfig.json
**/*.map
**/*.ts
!out/**
# Note: do NOT blanket-exclude node_modules — vsce already ships only the
# packages listed under "dependencies" (not devDependencies). Excluding
# everything here would strip runtime deps (posthog-node, etc.) and silently
# break extension activation.
scripts/**
